services:
  mysql:
    image: ecommerce_mysql
    container_name: ecommerce_db
    ports:
      - "3333:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: